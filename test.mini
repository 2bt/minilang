qword_at():
	asm:
		mov rax, QWORD PTR [rdi + rsi * 8]

byte_at():
	asm:
		mov cl, BYTE PTR [rdi + rsi]
		movzx rax, cl


main(argc, argv):
	var x

	printf("%d\n", 3 & 7);

	x = 0
	while x != argc:
		printf("%2d: %s\n", x, qword_at(argv, x))
		x = x + 1
	return 0
