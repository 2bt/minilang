

# inline asm
qword_at():asm:mov rax, QWORD PTR [rdi + rsi * 8]

byte_at():
	asm:
		mov		cl, BYTE PTR [rdi + rsi]
		movzx	rax, cl


main(argc, argv, x):

	x = 1	# we can't declare local variables yet
	printf("%d\n", 23 * 42)
	printf("%s\n", qword_at(argv, 0))
	return 0

