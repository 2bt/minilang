
# inline asm
qword_at():asm:
		mov rax, QWORD PTR [rdi + rsi * 8]

byte_at():asm:
		mov cl, BYTE PTR [rdi + rsi]
		movzx rax, cl

# we can't declare local variables yet
main(argc, argv, x, y):


	x = 0
	while x - argc:
		printf("%2d: %s\n", x, qword_at(argv, x))
		x = x + 1


	return 0

